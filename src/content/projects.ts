import type { Project } from './types'

/**
 * TODO: [Project write-ups]
 *
 * No specific professional or personal project has enough detail yet
 * (problem, architecture, decisions, outcome) to write an honest case
 * study — only a tech stack and role summary were provided. Rather than
 * inventing outcomes or metrics, this list starts empty.
 *
 * To add a project: fill in a Project object per src/content/types.ts.
 * Private/professional work can skip source and stay high-level — the
 * type intentionally has no field for exposing private code.
 */
export const projects: Project[] = []
